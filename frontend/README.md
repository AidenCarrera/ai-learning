## AI-Learning Frontend

Next.js 15 + TypeScript + TailwindCSS UI for uploading a PDF or text, selecting a mode (flashcards/quiz/test), and rendering mock results from the FastAPI backend.

### Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Make sure the backend is running at `http://localhost:8000`.
